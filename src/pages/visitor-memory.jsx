import JourneySolutionPage, { journeyPages } from "@/components/PageSections/JourneySolutionPage";

export default function VisitorMemoryPage() {
  return <JourneySolutionPage page={journeyPages["visitor-memory"]} />;
}
